schema_id: news-novelty-v1

skip_rules:
  min_event_intensity: 0.75
  max_price_action_recap_share: 0.80
  min_high_severity_clusters: 2

weights:
  novelty: 0.40
  event_intensity: 0.35
  sentiment: 0.25
  divergence: 0.00

filters:
  exclude_event_types: ["PRICE_ACTION_RECAP"]
  min_cluster_size: 2

notes: "High-precision baseline. Conservative skip. Pure news signal (no divergence)."
